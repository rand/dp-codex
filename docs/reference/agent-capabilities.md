# Agent Capabilities

`dp agent capabilities --json` returns ToolCards and schema versions.

The payload is intentionally compact and capped by tests. It is a local command manifest for agents,
not a full manual. Workflow detail belongs in runbooks and focused skills.
