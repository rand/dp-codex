---
name: dp-session-handoff
description: End or resume a session; trigger on handoff, compact, stop, session end.
---

# dp-session-handoff

Respect `AGENTS.md` and any nested `AGENTS.md` before using this skill.

When to use: Use before context compaction or final session closeout.

When not to use: unrelated repositories that have not opted into dp-codex.

First command: Run `dp agent bootstrap --json --detail brief` and record current dp status.

Keep work scoped, prefer compact JSON detail first, and expand only when needed.
