---
name: dp-adoption-migration
description: Adopt or migrate old dp projects; trigger on upgrade, legacy dp, migration.
---

# dp-adoption-migration

Respect `AGENTS.md` and any nested `AGENTS.md` before using this skill.

When to use: Use to plan additive adoption without erasing history.

When not to use: unrelated repositories that have not opted into dp-codex.

First command: Run `dp adopt inspect --json` before planning or applying changes.

Keep work scoped, prefer compact JSON detail first, and expand only when needed.
